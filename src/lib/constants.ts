/**
 * The catch-all event type that matches all events.
 * This is used to allow listeners to be added for all events.
 * It is not a standard event type and is used internally in the library.
 * @internal
 */
export const CATCH_ALL_EVENT_TYPE = "*";
